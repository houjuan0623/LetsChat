{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Services with PM2",
            "type": "shell",
            "command": "npm run dev",
            "problemMatcher": "$tsc-watch",
            "isBackground": true,
            "options": {
                "cwd": "${workspaceFolder}/ee/server/services"
            }
        }
    ]
}